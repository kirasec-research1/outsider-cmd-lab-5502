pull_request_rules:
  - name: outsider-injected-rule
    conditions:
      - base=main
    actions:
      comment:
        message: |
          FORK-CONFIG-EXECUTED-CANARY-5502
      label:
        add:
          - outsider-injected-label
